pulic interface Comparable<Item> {
    public int compareTo(Item that); 
}

    public class File implements Comparable<File> {
        ...
    }

Built-in Integer, String, Double, java.util.Date, java.io.File

v.compareTo(w)  returns
    -1 : v < w
    0 : v == w
    1 : v > w
    null : if types incompatible or null

Insertion Sort
    public static void sort(Comparable[] a) {
        int N = a.length;
        for (int i = 0; i < N; i++) {
            for (int j = i; j > 0; j--) {
                if (a[j].compareTo(a[j - 1]) < 0) {
                    exch(a, j, j-1);
                }
                else break;
            }
        }
    }